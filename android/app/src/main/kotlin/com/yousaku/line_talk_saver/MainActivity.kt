package com.yousaku.line_talk_saver

import io.flutter.embedding.android.FlutterFragmentActivity

// local_auth requires a FragmentActivity for its biometric/device-credential
// prompt (see local_auth_android's setup docs).
class MainActivity : FlutterFragmentActivity()
